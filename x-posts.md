# Kage — X (Twitter) Posts

> **Fact-check note for editors.** Before posting, cross-check claims against [docs/MODULE-REALITY.md](docs/MODULE-REALITY.md). In particular:
> - "ZK-proven reputation" applies *only* to the SP1 reputation circuit + `verify_sp1_proof` pipeline (via `zk.ts` / `/zk/commit/reputation`). The SDK's `reputation.ts` module is a local scoreboard, not a cross-agent provable system.
> - "On-chain" currently means one of three things: (a) Kage Anchor program state (vault, memories, grants, credentials, ZK verifications), (b) native `SystemProgram` transfers (stealth payments), or (c) SPL Memo program logs (timestamped audit trail, not contract-enforced). Phrase carefully.
> - "Shielded task delegation" — payload encryption is real end-to-end; task-lifecycle state is currently off-chain + memo log, not contract-enforced.
> - "Team vault membership" — contents are real (Shamir + AES-GCM); member removal does not rotate keys today.

---

## 1. ROADMAP ANNOUNCEMENT THREAD

---

### Tweet 1/6 — Hook

We've shipped 6 phases of Kage — encrypted agent memory, ZK proofs, shielded delegation, Shamir vaults, stealth payments, DID identity.

Today we're publishing Phases 7-9.

Here's what's next for the Shadow Memory Protocol 🧵

---

### Tweet 2/6 — Phase 7: Foundation

Phase 7 — Foundation

Production access control. Ed25519 DID signing. WebSocket messaging. Full test coverage. Mainnet prep.

Not glamorous. But if your agent protocol can't survive a security audit, nothing else matters.

We're hardening before we scale.

---

### Tweet 3/6 — Phase 8: Adoption

Phase 8 — Adoption

LangChain and CrewAI wrappers so any agent framework gets encrypted memory and ZK proofs out of the box.

Phantom/Solflare wallet connect. Token-gated API. Live demo agents.

Making $KAGE accessible to every AI developer.

---

### Tweet 4/6 — Phase 9: Horizon

Phase 9 — Horizon

Light Protocol for Solana-native shielded transactions. On-chain ZK circuit registry. Agent marketplace. Private semantic memory search.

This is where agent privacy becomes native infrastructure.

#AI #Solana

---

### Tweet 5/6 — Why This Matters

Why does this matter?

Every AI agent framework today stores memories in plaintext. Task data is visible. Communication is unencrypted.

Kage gives agents privacy as a primitive — not a feature, not an add-on.

Encrypted by default. Provable by design.

---

### Tweet 6/6 — CTA

$KAGE is live on pump.fun. Code is open source. SDKs ship in TypeScript and Python. The roadmap is public.

If you're building AI agents that handle sensitive data, this was built for you.

github.com/ranulfmeier/kage

Follow @kage_agent

---

## 2. STANDALONE POSTS

---

### Post 1 — ZK Proofs for AI Agents

Your AI agent completed a task. How do you prove it without exposing the data?

SP1 ZK proofs (Groth16) verified on-chain on Solana via `verify_sp1_proof`. The SP1 circuits cover memory integrity, task completion, and reputation — proofs are generated by a hosted prover (Succinct Network) and verified by the Kage Anchor program.

Zero-knowledge. Full accountability. #ZKProofs

---

### Post 2 — Why AI Agents Need Encrypted Memory

Your AI agent remembers everything — preferences, conversations, financial data, private instructions.

Where does it store that? A plaintext database anyone can query.

Kage encrypts every memory with AES-256-GCM before it leaves your device. Always.

---

### Post 3 — Shielded Task Delegation

Agent A delegates a task to Agent B.

In most frameworks, the payload — instructions, context, data — is fully visible.

With Kage, task payloads are encrypted end-to-end (X25519 ECDH + AES-256-GCM). Only the assigned agent can decrypt. Completion can be anchored with an SP1 task proof verified on-chain.

Shielded delegation.

---

### Post 4 — The Privacy Problem

AI agents today are glass boxes. Their memory, reasoning, task data — all exposed.

Now imagine fleets of agents handling medical records, financial decisions, legal analysis.

Privacy isn't a nice-to-have. It's a prerequisite for real-world agent deployment. #AIAgents

---

### Post 5 — LangChain Integration Teaser

Building AI agents with LangChain or CrewAI?

We're shipping native wrappers so your agents get encrypted memory and ZK proofs — without changing your existing stack.

Privacy as a plugin. Coming in Phase 8.

$KAGE

---

### Post 6 — Agent-to-Agent Encrypted Messaging

When two AI agents communicate, who's listening?

In Kage, agent-to-agent messaging is end-to-end encrypted. DID-based identity ensures you're talking to the right agent. No intermediary sees the content.

Private coordination for autonomous agents.

---

### Post 7 — Why Solana

Why build agent infrastructure on Solana?

Sub-second finality. Low fees for high-frequency memory commits. Groth16 proof verification on-chain. Composable ecosystem.

Agents need fast, cheap, verifiable infrastructure. Solana delivers. #Solana

---

### Post 8 — $KAGE Token Utility

$KAGE isn't governance theater.

Hold tokens, get API access. Four tiers:
- Shadow (10K) — basic
- Phantom (100K) — pro
- Specter (500K) — unlimited
- Kage (1M) — enterprise

No staking. No locking. Just hold in your wallet. Token-gated access to privacy infrastructure.

---

### Post 9 — Did You Know

Did you know AI agent memories can be tampered with?

If the memory store isn't cryptographically committed, anyone with DB access can rewrite an agent's history.

Kage commits every memory hash to Solana. Tamper with the data, the on-chain hash won't match.

---

### Post 10 — Provocative Question

What happens when AI agents can prove things without revealing data?

Prove task completion without exposing instructions. Prove reputation without leaking history. Prove identity without sharing keys.

That's what ZK proofs unlock for autonomous agents. We're building it.

---

## 3. PHASE 九 — HORIZON THREAD

---

### Tweet 1/7 — Hook

Phase 8 shipped. Kage is now plugged into LangChain, CrewAI, ElizaOS, Phantom, Solflare and token-gated by $KAGE tiers.

Phase 九 (Horizon) is live — and it's the most ambitious chapter yet.

Here's what "agent privacy as native infrastructure" looks like 🧵

---

### Tweet 2/7 — What's In Phase 九

Phase 九 — Horizon. Four pillars:

→ Light Protocol (Solana-native shielded TX)
→ On-chain ZK circuit registry
→ Agent discovery & marketplace ✅
→ Private semantic memory search (ZK + embeddings)

One is already shipped. Three are in progress. Let me break it down.

---

### Tweet 3/7 — Agent Marketplace (Shipped)

✅ Agent discovery & marketplace is LIVE.

Search, filter, review and hire autonomous agents — all running on Kage's privacy stack.

Every listed agent ships with encrypted memory, SP1-provable reputation (via the `verify_sp1_proof` pipeline), and on-chain-verified DID credentials.

The hiring layer for private agents.

---

### Tweet 4/7 — Light Protocol

Next up: Light Protocol integration.

Solana-native shielded transactions. Compressed state. Private balances and transfers built directly into the L1 you already use.

Kage + Light = agents that transact privately at Solana speed, without bridging to a separate privacy chain.

---

### Tweet 5/7 — On-Chain Circuit Registry

ZK circuits aren't static. They get patched, upgraded, deprecated.

We're building an on-chain registry so every verifier knows exactly which circuit version produced which proof.

Versioned ZK. Auditable upgrades. No silent trust assumptions.

---

### Tweet 6/7 — Private Semantic Memory Search

The hardest one: private semantic search.

Your agent should query its encrypted memory by meaning — not just by key — without ever decrypting it server-side.

ZK proofs + embeddings over ciphertext. Recall without exposure. This is new territory.

---

### Tweet 7/7 — Why Horizon Matters

Phases 壱-八 made agent privacy possible.

Phase 九 makes it native.

Shielded TX, versioned circuits, a marketplace, and searchable encrypted memory — this is the substrate autonomous agents will actually run on.

Follow @kage_agent
github.com/ranulfmeier/kage

---

## 4. PHASE 九 STANDALONE POSTS

---

### Post 11 — Marketplace Launch

The Kage agent marketplace is live.

Discover autonomous agents. Hire agents whose reputation is backed by SP1 Groth16 proofs verified on Solana — not just a local scoreboard.

Every agent on the marketplace ships with encrypted memory and on-chain-verified DID credentials by default.

Phase 九 has begun.

---

### Post 12 — Light Protocol Teaser

Solana-native shielded transactions are coming to Kage.

Light Protocol integration means agents pay, receive and settle privately — on the same L1 they already live on. No bridges. No wrapped assets.

Private value transfer for autonomous agents. $KAGE

---

### Post 13 — Versioned ZK

ZK circuits evolve. Bugs get patched. Constraints tighten.

Without versioning, verifiers can't tell a legitimate upgrade from a silent downgrade.

Kage's on-chain circuit registry pins every proof to a specific circuit hash. Provable lineage. No silent trust.

---

### Post 14 — Semantic Search Over Encrypted Memory

Plaintext vector DBs leak every query you make.

We're building semantic memory search that runs over encrypted embeddings — with ZK proofs that the recall was honest.

Your agent finds what it needs. Nobody else sees what it asked.

Phase 九.

---

### Post 15 — The Horizon Chapter

Six phases to build it. Two phases to harden it. One phase to ship it to the ecosystem.

Phase 九 is where Kage stops being a privacy *layer* and becomes privacy *infrastructure* — the default substrate autonomous agents run on.

Marketplace. Shielded TX. Versioned ZK. Private recall.

---

## 5. HARDENING DROP — TECHNICAL POST

---

### Post 16 — Prover Service Hardening

Quiet week, loud diff. The Kage prover service just got production-grade guardrails:

→ Per-IP rate limiting via `tower_governor` — 10 req/s burst, 30/s sustained, tunable via `PROVER_RATE_PER_SECOND` / `PROVER_RATE_BURST`.

→ Mandatory auth in network mode. If `PROVER_ENFORCE_AUTH=true` or the prover runs against SP1's network prover, missing `PROVER_API_KEY` is now a hard exit(2) — not a warning.

→ Route layering split: `/health` stays public, `/prove/*` and `/proof/{id}` sit behind auth + governor.

No more "oops, we left the prover open on port 3003." #ZK #Rust

---

### Post 17 — API Test Mode

Shipping tests for an Express app that boots an LLM client, opens a WebSocket, and pre-inits a KageAgent on module load is painful.

New in the API package: `KAGE_API_TEST_MODE=1`.

- Skips LLM provider bootstrap (uses a no-op stub)
- Skips `httpServer.listen`
- Skips agent pre-init
- Exports `app` + `httpServer` so supertest can mount them directly

Vitest configs landed for `@kage/api` and `@kage/agent`. CI is finally honest.

---

### Post 18 — SECURITY.md & Threat Model

We published [docs/SECURITY.md](docs/SECURITY.md) — the first written threat model for Kage.

Trust table across all six components (Solana program, SDK, prover, API, IPFS, user keypair). What each is trusted for, what it is *not* trusted for.

Explicit: plaintext confidentiality is the SDK's job, not IPFS's. Commitment integrity is the program's job, not the prover's. Users trust their own keypair above everything.

No hand-waving. If you want to audit Kage, start here.

---

### Post 19 — DEPLOYMENT.md

[docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) is live — full checklist for bringing up the Kage stack on devnet or mainnet.

Toolchain pins (Node 20+, Rust 1.82+, Anchor 0.32.0, SP1 6.0.2). Every required env var. API ↔ prover wiring. Groth16 verifier deploy. Keypair hygiene.

Built so a new operator can stand up the whole stack without reading a single source file.

---

### Post 20 — Why Hardening Beats Features

No new circuits this week. No marketplace polish. No fresh wrappers.

Just: rate limits, mandatory auth, test mode, threat model, deploy guide.

Because the thing that kills privacy protocols isn't a missing feature — it's an unauthenticated prover endpoint, a test suite nobody can run, and a threat model nobody wrote down.

Boring on the timeline. Load-bearing for mainnet.
